## Summary
> Machine Learning is Beautiful!
'''
import numpy as np
class_mapping={label: idx for idx,label in enumerate( np.unique( data_value_x['classname1'] ))}
class_mapping
data_value_x['classname1'].map(class_mapping)
'''

Git is a version control system.
Git is free software.
New Version.2
Git has a mutable index called stage.
Git tracks changes.
